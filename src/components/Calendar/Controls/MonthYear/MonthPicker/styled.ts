import { PickerGrid } from 'components/Calendar/Controls/MonthYear/styled';
import styled from 'styled-components';

export const MonthPickerGrid = styled(PickerGrid)`
  max-height: 250px;
  height: 100%;
  margin-top: 20px;
`;
